package sqlcmd.controller.command;

import sqlcmd.model.DatabaseManager;
import sqlcmd.view.View;

public class IsConnectedTest implements CommandTest {
    public IsConnectedTest(DatabaseManager manager, View view) {

    }
}
