package sqlcmd.controller.command;

import sqlcmd.model.DatabaseManager;
import sqlcmd.view.View;

public class FindTest implements CommandTest {
    public FindTest(DatabaseManager manager, View view) {

    }
}
