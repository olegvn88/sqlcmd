package sqlcmd.controller.command;

import sqlcmd.model.DatabaseManager;
import sqlcmd.view.View;

public class ClearTest implements CommandTest {
    public ClearTest(DatabaseManager manager, View view) {

    }
}
