package sqlcmd.controller.command;

import sqlcmd.model.DatabaseManager;
import sqlcmd.view.View;

public class ConnectTest implements CommandTest {
    public ConnectTest(DatabaseManager manager, View view) {

    }
}
